/**
 * shared/support/ticket-model.js
 *
 * Placeholder for support-ticket model helpers (buildTicket, buildReply, shape).
 * Real implementation comes next.
 */

const ticketModel = {
    isPlaceholder: true
};

if (typeof module !== "undefined" && module.exports) {
    module.exports = ticketModel;
}

if (typeof window !== "undefined") {
    window.ticketModel = ticketModel;
}
