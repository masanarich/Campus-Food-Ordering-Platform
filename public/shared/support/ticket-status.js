/**
 * shared/support/ticket-status.js
 *
 * Placeholder for ticket status constants and transitions.
 * Real implementation comes next.
 */

const ticketStatus = {
    isPlaceholder: true
};

if (typeof module !== "undefined" && module.exports) {
    module.exports = ticketStatus;
}

if (typeof window !== "undefined") {
    window.ticketStatus = ticketStatus;
}
